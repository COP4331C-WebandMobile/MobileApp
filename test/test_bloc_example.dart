

import 'package:flutter_test/flutter_test.dart';

void main() 
{
  group("ExampleBloc", ()
  {
    // blocClass bloc;
    setUp((){
      // bloc = blocClass();
    });

    tearDown((){
      // bloc.close();
    });

    test("Some description for the specific state of the bloc to be testing...", (){
      // expect(bloc.state, (){ });
    });

  }
  );
}